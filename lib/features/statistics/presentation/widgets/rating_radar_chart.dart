import 'package:flutter/material.dart';

class RatingRadarChart extends StatelessWidget {
  const RatingRadarChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 200, child: Placeholder());
  }
}
